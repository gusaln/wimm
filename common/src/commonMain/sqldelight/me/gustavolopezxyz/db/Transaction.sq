selectPaginated:
SELECT * FROM moneyTransaction LIMIT :limit, :offset;

selectById:
SELECT * FROM moneyTransaction WHERE transactionId = :transactionId;

selectByNumber:
SELECT * FROM moneyTransaction WHERE number = :number;

insertTransaction:
INSERT INTO moneyTransaction(number, categoryId, description)
VALUES (:number, :categoryId, :description);

updateTransaction:
UPDATE moneyTransaction
SET categoryId = :categoryId, description = :description
WHERE transactionId = :transactionId;

deleteTransaction {
    DELETE FROM entry WHERE transactionId = :transactionId;
    DELETE FROM moneyTransaction WHERE transactionId = :transactionId;
}